import React from 'react';
import {View} from 'react-native';

export class NotificationComponent extends React.Component<any, any> {
  constructor(props: unknown | Readonly<unknown>) {
    super(props);
  }

  render() {
    return <View />;
  }
}
