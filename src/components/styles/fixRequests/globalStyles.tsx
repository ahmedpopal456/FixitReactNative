import { StyleSheet } from 'react-native';

const GlobalStyles = StyleSheet.create({
  boldTitle: {
    fontWeight: 'bold',
  },
  flexRow: {
    flexDirection: 'row',
  },
});

export default GlobalStyles;
